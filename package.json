{
  "name": "frontrowmd-dashboard-api",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && npx wrangler deploy",
    "dev": "node build.js && npx wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.88.0"
  }
}
